import type { NextPage } from "next";
import Head from "next/head";
import Header from "../components/Header";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Hulu</title>
      </Head>
      <Header />
      {/* NavBar */}
      {/* Results */}
    </div>
  );
};

export default Home;
